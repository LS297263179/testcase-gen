services:
  app:
    build: .
    container_name: testcase-gen
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
